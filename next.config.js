/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['api-ninjas.com']
    },
    experimental: {
        appDir: true,
      },
}



module.exports = nextConfig
